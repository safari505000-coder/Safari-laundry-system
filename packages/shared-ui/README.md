# Safari ERP Shared UI

Reserved package for reusable customer/mobile UI tokens and components.

Use this only when duplication appears between:

- `apps/public-web`
- `apps/customer-mobile`
- `apps/employee-mobile`

Do not move internal admin components from `web` into this package unless they are truly product-neutral.
