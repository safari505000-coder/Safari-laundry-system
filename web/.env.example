# Development: leave unset so the Vite dev server proxies /api → http://localhost:3000
# Production build: set the public API origin (no trailing slash), e.g.:
# VITE_API_URL=https://erp-api.example.com
