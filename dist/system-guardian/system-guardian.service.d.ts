import { OnModuleInit } from "@nestjs/common";
import { CashMonitorService } from '../cash-monitor/cash-monitor.service';
import { CashRiskService } from '../cash-monitor/cash-risk.service';
import { CashExecutiveService } from '../cash-monitor/cash-executive.service';
import { IntegrityAuditService } from '../cash-monitor/integrity-audit.service';
import { SystemVerifyService } from '../cash-monitor/system-verify.service';
import { OwnerAlertNotifierService } from './owner-alert-notifier.service';
import { GuardianResponseDto, GuardianStatusResponseDto } from './dto/system-guardian.dto';
export declare class SystemGuardianService implements OnModuleInit {
    private readonly monitor;
    private readonly risk;
    private readonly executive;
    private readonly integrity;
    private readonly verify;
    private readonly notifier;
    private readonly logger;
    private inProgress;
    private lastResult;
    private readonly history;
    private readonly stableState;
    private readonly seenInPreviousSweep;
    constructor(monitor: CashMonitorService, risk: CashRiskService, executive: CashExecutiveService, integrity: IntegrityAuditService, verify: SystemVerifyService, notifier: OwnerAlertNotifierService);
    onModuleInit(): Promise<void>;
    scheduledSweep(): Promise<void>;
    runOnce(): Promise<GuardianResponseDto>;
    status(): Promise<GuardianStatusResponseDto>;
    private sweep;
    private runIntegrityCheck;
    private buildFromIntegrity;
    private mapIntegrityType;
    private runRegressionGuard;
    private regressionIssue;
    private runApiHealth;
    private latencyIssue;
    private runQueueHealth;
    private unstableIssue;
    private applyDedupAndEscalation;
    private aggregateSeverity;
    private shouldShip;
    private gcStableState;
    private formatMessage;
    private recommendAction;
    private emptyOk;
}
