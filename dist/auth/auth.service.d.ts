import { JwtService } from "@nestjs/jwt";
import { SafariRole } from "@prisma/client";
import { FinanceService } from '../finance/finance.service';
import { PrismaService } from '../prisma/prisma.service';
import { OperatingHoursService } from '../system/operating-hours.service';
import { UsersService } from '../users/users.service';
import { BcryptService } from './bcrypt.service';
import { ChangePasswordBodyDto } from './dto/change-password-body.dto';
import { LoginDto } from './dto/login.dto';
import { LoginResponseDto } from './dto/login-response.dto';
import { RefreshTokenResponseDto } from './dto/refresh-token.dto';
export declare const INSTITUTIONAL_ROLES: readonly SafariRole[];
export declare class AuthService {
    private readonly prisma;
    private readonly jwt;
    private readonly financeService;
    private readonly bcryptService;
    private readonly operatingHours;
    private readonly usersService;
    private readonly logger;
    constructor(prisma: PrismaService, jwt: JwtService, financeService: FinanceService, bcryptService: BcryptService, operatingHours: OperatingHoursService, usersService: UsersService);
    login(dto: LoginDto): Promise<LoginResponseDto>;
    changePassword(userId: string, dto: ChangePasswordBodyDto): Promise<LoginResponseDto>;
    refreshAccessToken(rawToken: string): Promise<RefreshTokenResponseDto>;
    revokeRefreshToken(rawToken: string): Promise<void>;
    private buildLoginUserDto;
    private issueAuthenticatedSession;
    private issueRefreshToken;
    private recordOutsideHoursAudit;
}
