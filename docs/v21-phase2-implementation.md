# V21 — PHASE 2 — CLEANUP IMPLEMENTATION REPORT

> Sister doc to `v21-phase2-legacy-audit.md`. Records the four
> cleanup waves executed during V21 Phase 2, the validation gate
> result after each wave, and the additive build-fail guard that
> locks the cleanup in.

## 1 — Waves executed

### Wave 1 — Frontend orphan source files

Deleted 4 source files (zero importers, classified `SAFE_REMOVE`
in §2.1 of the audit):

| File | Bytes | Lines | Era |
|------|------:|------:|-----|
| `web/src/modules/call-center/collections-report/utils/grouping.ts` | 5,279 | 148 | V20.7 |
| `web/src/modules/shared/components/onboarding/OnboardingTour.tsx` | 4,599 | ~100 | V20.9 Phase 4 |
| `web/src/modules/shared/hooks/use-responsive-mode.ts` | 2,069 | 57 | V20.9 Phase 4 |
| `web/src/modules/shared/routing/lazy-route.tsx` | 1,752 | 56 | V20.7 Phase 6 |

**Total deleted**: 4 files, 13,699 bytes, ~361 lines.

**Validation gate**: `npx vitest run` → **139 / 139 ✅**, `npm run build` → **clean**.

### Wave 2 — Placeholder folder

Removed `web/src/modules/callcenter/README.md` and the empty
folder. Classification: `SAFE_REMOVE` in §2.2 (the V20.8 expanded
UI consistency guard uses `safeStat` so missing folders are
gracefully skipped).

**Validation gate**: `npx vitest run src/modules/finance/components/v20-8-ui-consistency-expanded.test.ts src/modules/finance/components/v20-9-module-only-architecture.test.ts src/modules/finance/cross-module-import-guard.test.ts` → **5 / 5 ✅**.

### Wave 3 — `dist/` git untracking

1. Added a `dist/` block (and `web/dist/` for safety) to `.gitignore`.
2. `git rm -r --cached dist` → 1,479 files removed from the git index.
3. The local `dist/` working-tree folder remains untouched; it is regenerated by `nest build` / `tsc -p tsconfig.build.json`.

| Before | After |
|-------:|------:|
| 1,479 tracked `dist/` files | **0** |

**Validation gate**: `npx nest build` → **exit 0** (`dist/` regenerated locally).

### Wave 4 — Additive build-fail guard

Added `web/src/modules/finance/v21-phase2-cleanup-guard.test.ts`
with **7 tests**:

- 4 × removed-file resurrection guards (one per Wave 1 deletion)
- 1 × removed-folder resurrection guard (Wave 2 placeholder)
- 1 × raw `window.fetch(...)` discipline guard (tree-wide ban
  with allowlist for `lib/api.ts`, `offline/flush-queue.ts`, and
  `modules/<m>/api/` + `modules/<m>/state/` files)
- 1 × `dist/` git-ignore presence guard

**Validation gate**: `npx vitest run src/modules/finance/v21-phase2-cleanup-guard.test.ts` → **7 / 7 ✅**.

## 2 — Aggregate impact

| Concept                                | Before V21 P2 | After V21 P2 |
|----------------------------------------|--------------:|-------------:|
| `web/src/` source files (madge)         | 365           | **362**      |
| Tracked `dist/` files in git            | 1,479         | **0**        |
| Untouched (working-tree) `dist/` files  | 1,731         | 1,731 (regenerated) |
| Frontend test count                     | 139           | **146** (+7) |
| Frontend test files                     | 26            | **27** (+1)  |
| Frontend production build               | clean         | clean        |
| Frontend circular dependencies          | 0             | **0**        |
| Backend test count                      | 723 passing   | **723 passing** |
| Backend production build                | clean         | clean        |
| Lines of dead code removed (frontend)   | 0             | **~361**     |

## 3 — Sub-audits (Section 3-7 of the mission spec)

### Section 3 — Design System Consolidation

**Status**: Already at zero-duplicate baseline post-V20.7-V20.8.
See `docs/v21-phase2-design-system-state-api-audit.md §1` for the
canonical primitive catalogue and the 8 lock-in test files.

**Action this phase**: none beyond verification. No consolidation
work needed because every primitive class (badge, KPI card, table,
loader, skeleton, empty state, timeline card, status chip, header,
financial widget) already resolves to a single canonical component.

### Section 4 — State + Cache Normalization

**Status**: Already canonical post-V20.8-V20.9 + V21 Phase 1.
The cross-module finance-internal-import guard added in Phase 1
remains the boundary anchor. See
`docs/v21-phase2-design-system-state-api-audit.md §2`.

**Action this phase**: none beyond verification.

### Section 5 — API Layer Consolidation

**Status**: Single-fetch-channel guarantee holds (`v20-8-no-direct-fetch.test.ts`),
extended in this phase by the new tree-wide raw-`fetch(` ban.
See `docs/v21-phase2-design-system-state-api-audit.md §3`.

**Action this phase**: added the raw-`fetch(` discipline guard
(in the new `v21-phase2-cleanup-guard.test.ts`).

### Section 6 — Import + Module Boundaries

**Status**: Locked by:
- `v20-9-module-only-architecture.test.ts` (legacy → modules direction)
- `v21-phase1-cross-module-import-guard.test.ts` (no deep imports into `modules/finance/{state,api,hooks}/*`)
- `v20-8-ui-consistency-expanded.test.ts` (no deep relative cross-module imports under `modules/<x>/{components,pages}`)
- `madge --circular web/src` → 0 cycles
- `madge --circular src` → 8 `forwardRef`-resolved settlement-triangle cycles (documented intentional design, V21 Phase 4)

**Action this phase**: none beyond verification.

### Section 7 — UI Consistency Normalization

**Status**: Locked by V20.7-V20.8-V20.9 design-system + UX-polish
+ command-palette tests. The V20.8 `v20-8-ui-consistency-expanded.test.ts`
enforces no client-side KD math under all enforced module roots,
and the V21 Phase 1 `moneyComparisonGuardedFiles` allowlist locks
14 specific frontend display files into the canonical comparison
regime.

**Action this phase**: none beyond verification.

## 4 — Files added

| File | Purpose | Tests |
|------|---------|------:|
| `docs/v21-phase2-legacy-audit.md`                                | Legacy discovery audit + classification | n/a |
| `docs/v21-phase2-implementation.md`                              | This file | n/a |
| `docs/v21-phase2-deletion-inventory.md`                          | Per-file deletion record | n/a |
| `docs/v21-phase2-validation.md`                                  | Post-wave validation log | n/a |
| `docs/v21-phase2-rollback-guide.md`                              | Per-wave rollback procedure | n/a |
| `docs/v21-phase2-architecture-scorecard.md`                      | Final architecture scorecard | n/a |
| `docs/v21-phase2-design-system-state-api-audit.md`               | Sub-audit for objectives 3-5 | n/a |
| `web/src/modules/finance/v21-phase2-cleanup-guard.test.ts`       | 7 build-fail guards (resurrection, fetch, dist) | **7** |

## 5 — Files modified

| File | Change |
|------|--------|
| `.gitignore` | Added `dist/` + `web/dist/` block (8 lines incl. justification comment) |

## 6 — Files removed

5 files + 1 folder + 1,479 git-index entries (`dist/`).
See `docs/v21-phase2-deletion-inventory.md` for the complete
inventory.

## 7 — Hard-rule preservation matrix

| Hard rule | Mechanism |
|-----------|-----------|
| Do NOT modify canonical journal logic       | `src/general-ledger/double-entry-journal.service.ts` not touched; verified by V21 Phase 1 mutation-boundary suite |
| Do NOT modify settlement behaviour           | `src/customer-ledger/customer-ledger.service.ts` not touched; verified by 16 customer-ledger spec files all green |
| Do NOT modify historical financial data      | No Prisma migration generated; no `update`/`upsert` calls added; no rows touched |
| Do NOT delete runtime-used canonical services | All 5 deletions are confirmed-zero-importer files (madge) + grep-verified |
| Do NOT introduce API breaking changes        | No DTOs / controllers added or removed |
| Every deletion rollback-safe + validated     | Each wave gated by `vitest run` + production build before proceeding to next wave |
| Every cleanup preserves behaviour exactly    | Behavioural equivalence: 723 backend + 146 frontend tests green pre/post |
